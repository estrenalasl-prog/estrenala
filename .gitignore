# El Creador de Blog es un repo propio incluido como referencia/librería fuente.
Creador de Blog/

# Dependencias y builds
node_modules/
.next/
dist/
build/

# Datos locales y storage local (local-first).
# OJO: anclados a la raíz con "/" para NO ignorar el código fuente en src/storage/.
data/
/storage/

# Entorno
.env
.env.local
.env.*.local

# Artefactos de TypeScript / Next
next-env.d.ts
*.tsbuildinfo

# Estado local de Claude Code (permisos por máquina, no versionar)
.claude/

# Sistema
.DS_Store
Thumbs.db
*.log
